services:
  - type: web
    name: context-graph-system
    env: python
    plan: free
    buildCommand: pip install -r backend/requirements.txt && python scripts/download_dataset.py && python scripts/prepare_data.py
    startCommand: uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT
